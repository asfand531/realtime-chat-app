# Backend

cd back-end/
node --watch server.js

# Frontend

cd front-end/realtime-chat-app/
npm install
npm run dev

# Notes

- Ensure `sql/schema.sql` has been executed or the server will create tables on startup.
